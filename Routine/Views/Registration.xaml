<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Routine.Views.Registration">
    <ScrollView>
        <StackLayout VerticalOptions="Center"
                 Margin="20" x:Name="containerStackLayout" >

            <Entry x:Name="UsernameEntry" Placeholder="Username" Text ="{Binding Username, Mode=TwoWay}"/>

            <Entry x:Name="NameEntry" Placeholder="Name" Text ="{Binding Name, Mode=TwoWay}"/>

            <Entry x:Name="EmailEntry" Placeholder="Email" Keyboard="Email" Text ="{Binding Email, Mode=TwoWay}"/>

            <Entry x:Name="PasswordEntry" Placeholder="Password" IsPassword="True" Text="{Binding Password, Mode=TwoWay}"/>

            <Grid>
                <Button Text="Sign Up" Clicked = "SignUp_Clicked"/>
            </Grid>

        </StackLayout>

    </ScrollView>
</ContentPage>